[project]
name = "agentic-payments"
version = "0.1.0"
description = "An agent that autonomously pays small paywall fees via a crypto wallet, x402, or a fiat card bridge."
requires-python = ">=3.11"
dependencies = [
    "python-dotenv",
]

[project.optional-dependencies]
wallet = ["cdp-sdk"]
offramp = ["lithic"]
agent = ["anthropic"]
browser = ["playwright"]
payments = ["x402[requests]"]
tools = ["beautifulsoup4"]
ui = ["fastapi", "uvicorn[standard]", "jinja2", "python-multipart"]
documents = ["fpdf2"]
dev = ["pytest"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
